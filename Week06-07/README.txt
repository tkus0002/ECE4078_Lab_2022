NOTE:DO NOT RUN ON PYTHON 3.6

1.Change the directory of the command line to the current folder 
2.Install the requirments needed to run the model with the following command pip install -r requirements.txt
3.Intsall the following library locally % pip install machinevision-toolbox-python
4.Install the panda library
5.Ensure Pytorch is installed on the device using install pytorch
6.Move the best.pt to network/scripts/model if not already there
7.Move detector.py to network if not not already there.

