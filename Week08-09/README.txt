1.install the requirments needed to run the model with the following command pip install -r requirements.txt in the same folder as the files
2.Install python 3.7 if not already installed to avoid any issues
3. Model is sved as best.pt with a copy using the recommended naming
